[package]
name = "ci"
edition = "2024"
description = "Tool that enables running CI checks locally."
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
argh = "0.1"
xshell = "0.2"

[lints]
workspace = true
