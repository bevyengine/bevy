[package]
name = "build-tailwind-palette"
edition = "2024"
description = "Tool that generates the Tailwind CSS color palette"
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
bevy_color = { path = "../../crates/bevy_color" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
