[package]
name = "scene-viewer"
version = "0.1.0"
edition = "2021"
description = "Scene viewer tool for Bevy"
publish = false
license = "MIT OR Apache-2.0"

[features]
default = []
jpeg = ["bevy/jpeg"]

[dependencies]
bevy = { path = "../../" }
