[package]
name = "simple-ecs-test"
edition = "2024"

# We depend on bevy in both normal and dev dependencies to verify that the proc macros still work.

[dependencies]
bevy = { path = "../../" }

[dev-dependencies]
bevy = { path = "../../" }

[patch.crates-io]
wesl = { git = "https://github.com/tychedelia/wesl-rs", rev = "139768ad1bd03e1eb9ac236dac618eacd6542990" }
