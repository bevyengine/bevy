---
title: "InlineBox and InlineImage"
authors: ["@Ickshonpe"]
pull_requests: [25710]
---

`InlineBox` is a new component added to `bevy_text` that allows space to be reserved within text layouts for custom content. 

`InlineImage` is a new component added to `bevy_ui` that uses `InlineBox` to display images interspered with text. 

