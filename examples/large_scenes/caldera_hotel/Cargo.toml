[package]
name = "caldera_hotel"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { path = "../../../", features = ["bevy_camera_controller", "free_camera"] }
mipmap_generator = { path = "../mipmap_generator" }

argh = "0.1.12"
