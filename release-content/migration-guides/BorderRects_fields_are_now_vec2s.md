---
title: "`BorderRect`'s fields are now `Vec2`'s"
pull_requests: [21581]
---

The directional BorderRect fields (`left`, `right`, `top`, and `bottom`) have been replaced with `min` and `max` `Vec2` fields.
